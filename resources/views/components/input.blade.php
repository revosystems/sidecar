<input id="{{ $id }}" type="{{ $type }}" name="{{ $name }}"
       style="width: {{ $width ?? '300px' }}"
       value="{{ $value }}"
       class="h-7 px-2 text-sm text-gray-700 border rounded sidecar-input {{ $classes ?? '' }}"
>